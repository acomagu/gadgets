version: '3'
services:
  dicebot:
    image: acomagu/dicebot
  graphdb:
    image: acomagu/graphdb:8.8.1-free
    expose:
      - '7200'
  nginx:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - 80:80
